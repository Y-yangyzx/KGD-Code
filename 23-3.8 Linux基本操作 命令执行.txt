第三课：
    Linux操作系统的基本操作（图形界面操作、命令行操作）
1、	命令行操作Bash基础（bash：Linux中操作的命令）
shell相当于管理员用户和操作系统之间的媒介   shell分类：图形界面shell和命令行式shell
2、	Bash基础（平台内定的Shell）  用户通过Shell将输入的命令与内核进行通信，从而让内核控制硬件准确高效的完成工作
1）提示符：#：超级用户（使用root登录系统 权限比较大）   $：普通用户
  su命令：切换用户  语法：su 直接加用户名
             cd命令：跳转到哪个目录下  只敲一个cd跳到该用户的根目录
             pwd命令：查看当前工作目录
2）结构解读：[用户名@主机名~]#  @：表示使用那个用户登录到哪台主机  ~：表示当前所在目录
3）查看主机名字：hostname
修改主机名称：hostnamectl set-hostname 要修改后的名字
              输入密码
              bash
          4）Bash命令组成：$ 命令名称 [命令参数] [命令对象]
                              命令名称均为小写  []中的内容可选
                 /：代表Linux系统的最上层目录
          5）ls命令：罗列当前文件下所有目录（蓝色的是目录 黑色的是文件）  
                ls -al /：显示所有目录的信息
                ls -ail /：显示显示所有目录信息及节点号（比ls -al /多的就是节点号）
                                               节点号是唯一的 node ip
3、	Linux系统中常用命令
一些特殊目录：1>“.”表示当前目录
              2>“..”表示当前目录的上层目录
              3>“-”表示前一个工作目录
              4>“~”表示当前用户所在的根目录
               5>“linuxstudy”表示linuxstudy用户操作的常用命令
3.1查看目录
1）	切换目录
                 cd命令：切换目录  若啥也不写，切换至用户的根目录
                 使用方式：cd [目录名称]
                 实例：cd 等价于 cd~
                       cd ..切换到当前目录的上级目录
                       cd -：切换到前一个工作目录
                       cd /user/bin：绝对路径：从根目录开始寻址到当前目录为止
                       cd ./yum ：相对路径：表示切换到当前路径的yum下 ./表示 当前目录
2）	查看当前路径
3）	查看目录列表
           3.2管理和创建文件
            1）创建目录
            2）删除目录
            3）创建文件
           3.3赋值、移动和删除
            1）赋值命令
            2）移动命令
            3）删除命令
           3.4查看文件内容
            1）cat命令
            2）tac命令
            3）more命令
            4）less命令
            5）head命令
            6）tail命令
           3.5文本编辑器Vim
           3.6Linux系统中获取帮助
